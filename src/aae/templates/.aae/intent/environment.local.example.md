# Local Environment Example

<!-- Copy this tracked example to environment.local.md. The copied file is ignored by Git and overrides conflicting shared environment preferences for this execution environment. -->

This workstation uses its existing operating system, toolchain, and runtime configuration. Do not install or replace system-level dependencies automatically.

<!-- Record machine-specific capabilities, unavailable tools, private endpoint aliases, resource limits, or local workflow constraints here. Never include credentials or secrets. -->
