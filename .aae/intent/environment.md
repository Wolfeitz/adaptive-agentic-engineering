# Environment

The bootstrap must run with Python 3.10 or newer and no runtime dependencies. It must work locally, in GitHub Actions, and in existing repositories without overwriting unrelated files.
